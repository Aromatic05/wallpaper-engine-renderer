#pragma once

#include "runtime/backend/ContentBackend.hpp"

namespace wallpaper
{
// Placeholder for the future web backend. The runtime can depend on the
// ContentBackend interface today without treating scene as the architecture center.
class WebBackend;
} // namespace wallpaper
